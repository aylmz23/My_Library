@using My_Library.Entities;
@model IEnumerable<Library>
@{
    ViewData["Title"] = "Home Page";
    int counter = 0;
}
<div class="container-fluid" id="navbarView">
    <div class="row gx-2 gx-lg-3 row-cols-sm-1 row-cols-md-2 row-cols-lg-3 row-cols-xl-6  justify-content-center ">

        @foreach (var item in Model)
        {
            counter++;
            <div class="container ay-zoom radius">
                <div class="card my-3 bg-dark text-white radius" style="width:15rem">
                    <div class="ay_card_image align-self-center radius">
                        <img src="~/Img/@item.Image" class="my-1 " alt="..." id="viewImg">
                    </div>
                    <div class="card-body">
                        <p class="card-text ay_card_text-Name">@item.Name</p>
                        <p class="card-text ay_card_text-Author">@item.Author</p>
                        <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-warning cl-o col-12">Details <span class="position-absolute top-0 start-0 translate-middle badge rounded-pill bg-success"> @counter </span></a>
                    </div>
                </div>
            </div>
        }
        </div>
    </div>
<script>
    function BookDetails() {
        var myModal = new bootstrap.Modal(document.getElementById("myModal"));
        myModal.show();
    };
</script>